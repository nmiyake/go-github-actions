name: actions

on:
  push:
  release:
    types: [published]

jobs:
  verify:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - run: which go
      - run: ls -la /usr/bin/go
      - run: go env
      - id: set-gopath
        run: echo "::set-output name=GOPATH::$(go env GOPATH)"
      - run: |
          echo "GOPATH value: ${{ steps.set-gopath.outputs.GOPATH }}"
      - id: go-dist-version
        uses: ./.github/actions/go-dist-version
      - id: go-dist-exists
        shell: bash
        run: |
          GO_DIST_EXISTS=0
          if [ -d "${{ steps.set-gopath.outputs.GOPATH }}/go-dists/${{ steps.go-dist-version.outputs.go-dist-version }}" ]; then
            echo "Setting GO_DIST_EXISTS=true"
            GO_DIST_EXISTS=1
          fi
          echo "::set-output name=GO_DIST_EXISTS::$GO_DIST_EXISTS"
      - id: download-go-dist
        if: ${{ steps.go-dist-exists.outputs.GO_DIST_EXISTS == 0 }}
        uses: ./.github/actions/go-dist-download
        with:
          go-version: ${{ steps.go-dist-version.outputs.go-dist-version }}
      - run: |
          echo "Cache hit: ${{ steps.go-dist-exists.outputs.GO_DIST_EXISTS == 1 }}"
        shell: bash
        if: ${{ steps.go-dist-exists.outputs.GO_DIST_EXISTS == 1 }}

#      - uses: actions/cache@v2
#        if: !go-dist-exists
#        with:
#          path: |
#            ~/go/pkg/mod
#            ~/.cache/go-build
#            ~/Library/Caches/go-build
#            %LocalAppData%\go-build
#          key: ${{ steps.go-dist-version.outputs.go-dist-version }}
#      - if cache miss:
#          # download/install go dist at expected location
#      - set up symlink


#      - run: echo "Go dist version - ${{ steps.go-dist-version.outputs.go-dist-version }}"
#        shell: bash

#      - id: setup-go
#        uses: ./.github/actions/my-action
#        with:
#          gopath: /home/runner/go
#      - run: go version
#        with:
#          who-to-greet: 'Mona the Octocat'
#      - run: echo random-number ${{ steps.foo.outputs.random-number }}
#        shell: bash
#      - uses: actions/setup-go@v2
#        with:
#          go-version: '1.16.4'
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/go/pkg/mod
#            ~/.cache/go-build
#            ~/Library/Caches/go-build
#            %LocalAppData%\go-build
#          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/.godel
#          key: ${{ runner.os }}-godel-${{ hashFiles('godelw', 'godel/config/godel.yml') }}
#      - run: ./godelw verify --apply=false

#  dist:
#    runs-on: ubuntu-20.04
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-go@v2
#        with:
#          go-version: '1.16.4'
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/go/pkg/mod
#            ~/.cache/go-build
#            ~/Library/Caches/go-build
#            %LocalAppData%\go-build
#          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/.godel
#          key: ${{ runner.os }}-godel-${{ hashFiles('godelw', 'godel/config/godel.yml') }}
#      - run: ./godelw dist
#      - name: Upload dist artifacts
#        uses: actions/upload-artifact@v2
#        with:
#          name: dist-output
#          path: out
#
#  publish:
#    runs-on: ubuntu-20.04
#    if: ${{ github.event_name == 'release' && github.event.action == 'published' }}
#    needs:
#      - verify
#      - dist
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-go@v2
#        with:
#          go-version: '1.16.4'
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/go/pkg/mod
#            ~/.cache/go-build
#            ~/Library/Caches/go-build
#            %LocalAppData%\go-build
#          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
#      - uses: actions/cache@v2
#        with:
#          path: |
#            ~/.godel
#          key: ${{ runner.os }}-godel-${{ hashFiles('godelw', 'godel/config/godel.yml') }}
#      - name: Download dist artifacts
#        uses: actions/download-artifact@v2
#        with:
#          name: dist-output
#          path: out
#      - run: if [[ -d out ]]; then find out -exec touch {} \;; fi
#      - run: ./godelw publish github --add-v-prefix --api-url=$GITHUB_API_URL --user=${{ github.repository_owner }} --repository=${{ github.event.repository.name }} --token=${{ secrets.GITHUB_TOKEN }}
